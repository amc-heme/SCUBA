% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_feature.R
\name{fetch_feature}
\alias{fetch_feature}
\alias{fetch_feature.Seurat}
\alias{fetch_feature.SingleCellExperiment}
\alias{fetch_feature.AnnDataR6}
\title{Fetch feature expression data from single-cell objects}
\usage{
fetch_feature(object, features, assay = NULL, layer = NULL, cells = NULL)

\method{fetch_feature}{Seurat}(object, features, assay = NULL, layer = NULL, cells = NULL)

\method{fetch_feature}{SingleCellExperiment}(object, features, assay = NULL, layer = NULL, cells = NULL)

\method{fetch_feature}{AnnDataR6}(object, features, assay = NULL, layer = NULL, cells = NULL)
}
\arguments{
\item{object}{A single cell object. Currently, Seurat, SingleCellExpleriment,
and anndata objects are supported.}

\item{features}{A character vector of feature names to retrieve from the
object. Features should be specified with the assay key prefix if pulling
from a non-default assay (e.g., "rna_FLT3"). To determine the key that
corresponds to the assay to pull data from, run \code{\link[=all_keys]{all_keys()}}.}

\item{assay}{For Seurat objects, the assay to pull data from. If \code{NULL},
the default assay will be used.}

\item{layer}{The layer to pull data from. Layers are referred to as "slots"
in Seurat objects v4 and earlier, and "assays" in SingleCellExperiment
objects. If \code{NULL}, the default layer will be used.}

\item{cells}{A character vector of cell names to include, as they are named
in the object (i.e. according to \code{colnames(object)}). If \code{NULL}, data will
be returned for all cells in the object.}
}
\value{
A data.frame with the requested \code{features} as columns and the cells
as rows.
}
\description{
\strong{This function is still in development.} Currently, it only has a full
implementation for Seurat objects. For all other object types, it acts as
a wrapper around \code{\link[=fetch_data]{fetch_data()}}.

This function retrieves feature expression data from single-cell objects.
For Seurat objects with BPCells-backed assay layers, this function uses
optimized direct matrix access for improved performance. For all other
cases, it delegates to \code{\link[=fetch_data]{fetch_data()}}.

Unlike \code{\link[=fetch_data]{fetch_data()}}, this function is specifically designed for feature
retrieval and does not support fetching metadata or reduction coordinates.

See our GitHub.io website for additional information and examples.
}
\section{Methods (by class)}{
\itemize{
\item \code{fetch_feature(Seurat)}: Seurat objects. Uses optimized BPCells access
when available, otherwise delegates to \code{\link[=fetch_data]{fetch_data()}}.

\item \code{fetch_feature(SingleCellExperiment)}: SingleCellExperiment objects. Wrapper for
\code{\link[=fetch_data]{fetch_data()}}.

\item \code{fetch_feature(AnnDataR6)}: AnnDataR6 objects. Wrapper for \code{\link[=fetch_data]{fetch_data()}}.

}}
\examples{
# Fetch feature expression data
fetch_feature(AML_Seurat, features = c("FLT3", "NPM1")) |> head()

# Fetch from a specific layer
fetch_feature(AML_Seurat, features = "FLT3", layer = "data") |> head()

}
\keyword{internal}
