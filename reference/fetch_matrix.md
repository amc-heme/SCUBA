# Fetch matrix

Returns object metadata for a specified set of cells.

## Usage

``` r
fetch_matrix(object, matrix_location, ...)

# S3 method for class 'AnnDataR6'
fetch_matrix(object, matrix_location, ...)
```

## Arguments

- object:

  a single-cell object (Seurat, SingleCellExperiment, Anndata).

- matrix_location:

  the

- ...:

  Currently unused.

## Methods (by class)

- `fetch_matrix(AnnDataR6)`: AnnDataR6 objects
